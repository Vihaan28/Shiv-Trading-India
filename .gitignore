node_modules/
.netlify/
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# data.json and sitemap.xml ARE committed on purpose.
# They are generated by "npm run build", but keeping them in the repository
# means the site still works if someone opens it without running a build, and
# it makes catalogue changes visible in the Git history.
